# Warp Control CLI underlying-data read verification
See `manifest.json` for the full command-by-command verification manifest. Screenshots are under `screenshots/` and command/app logs are under `logs/`.
